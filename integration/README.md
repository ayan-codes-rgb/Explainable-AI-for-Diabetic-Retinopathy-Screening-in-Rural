# integration

**Owner: M6**

`analyzeImage()` — calls every module in order and returns the single shared result struct. Also holds end-to-end test scripts.

Must fill in the shared result struct: all of it — this is the function that assembles the struct

Read `../common/README.md` before writing any image code. Use `loadFundus()`, never `imread()`.
