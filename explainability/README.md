# explainability

**Owner: M5**

Grad-CAM attention maps, lesion-level evidence correlated with clinical criteria, calibrated confidence, and the automated annotated report.

Must fill in the shared result struct: `result.gradcamMap`

Read `../common/README.md` before writing any image code. Use `loadFundus()`, never `imread()`.
