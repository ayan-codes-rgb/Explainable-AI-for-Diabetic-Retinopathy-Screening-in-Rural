# segmentation

**Owner: M2**

Retinal structure segmentation: optic disc localisation, fovea localisation, vessel segmentation.

Must fill in the shared result struct: `result.opticDisc`, `result.fovea`, `result.vesselMask`

Read `../common/README.md` before writing any image code. Use `loadFundus()`, never `imread()`.
